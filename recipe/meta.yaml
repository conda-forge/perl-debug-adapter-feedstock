{% set version = "2.0.1" %}

package:
  name: perl-debug-adapter
  version: {{ version }}

source:
  url: https://github.com/Nihilus118/perl-debug-adapter/archive/v{{ version }}.tar.gz
  sha256: ae4a792d174b6aed4baa64512d479645cb30d31ef009e916914baea59c074913

build:
  noarch: generic
  number: 0

requirements:
  host:
    - nodejs
    - pnpm
    - pnpm-licenses
  run:
    - nodejs

test:
  commands:
    - perl-debug-adapter &
    - exe_pid=$!
    - trap "kill -SIGTERM ${exe_pid}" SIGTERM

about:
  home: https://github.com/Nihilus118/perl-debug-adapter
  license: MIT
  license_file:
    - LICENSE.md
    - third-party-licenses.txt
  summary: Debug adapter for Perl
  dev_url: https://github.com/Nihilus118/perl-debug-adapter

extra:
  recipe-maintainers:
    - danielnachun
